<h1><%= telephone.contact.fullname %></h1>

<%= form_for(telephone,{:url => [telephone.contact,telephone]}) do |f| %>
  <%= f.hidden_field :contact_id %>

  <% if telephone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(telephone.errors.count, "error") %> prohibited this telephone from being saved:</h2>

      <ul>
      <% telephone.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :phonenumber %>
    <%= f.text_field :phonenumber %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
